const express = require("express");
const router = express.Router();

// User routes go here later (the single demo user, saved/completed quests, ...).
// Not wired into server.js yet — this file is just a placeholder for now.

module.exports = router;
